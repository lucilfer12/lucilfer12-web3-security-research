id: zeromark-146
title: Unbounded penalty in executeForceExit
type: original-research
status: validated-fixed
disclosure: sanitized
category: privileged-input-validation
security_property: A privileged exit operation must not accept a penalty above the protocol intended maximum.
reproducible: true
target: CarthaParentVault.sol
entry_point: executeForceExit(address owner, uint256 penaltyBps)
actor: FORCE_EXITER
privilege: privileged-role
root_cause: Missing upper-bound validation for penaltyBps at the parent-vault boundary.
failure: penaltyBps=10000 can reduce payout to zero while shares are settled or burned.
impact: Potential loss of the affected user's vault payout under the required privileged-role condition.
mitigation: Add and enforce a parent-vault MAX_PENALTY_BPS guardrail.
stages: [observed, modeled, reproduced, mitigated]
invariants: [invariant.authorization.critical-state, invariant.protocol.transition-closure]
patterns: [pattern.unbounded-privileged-input]
counterexamples: [cx.authorization.penalty-upper-bound]
protocols: [protocol.0xmarkets.cartha]
evidence: [evidence.case.zeromark-146]
sources: [source.repo.central]
hypotheses: [hypothesis.authorization.input-boundary]
regressions: [regression.zeromark-146.penalty-bound]
provenance:
  - source: repository
    locator: case-studies/validated-findings/0xmarkets/zeromark-146/case.yaml
